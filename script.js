import { ui } from './js/ui.js';

// The main entry point just ensures the UI initializes.
// The event listener is already inside ui.js, but we can also put it here if we want to be cleaner.
// For now, ui.js has the DOMContentLoaded listener.
